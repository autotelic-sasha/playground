{
  "$id": "https://example.com/schemas/common/base-entity.json",
  "title": "BaseEntity",
  "type": "object",
  "x-graph": {
    "anchor": true,
    "x": 0,
    "y": 0
  },
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]+$"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "id",
    "createdAt"
  ]
}